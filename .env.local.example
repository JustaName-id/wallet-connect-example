# Get your API key from https://jaw.id (JAW dashboard)
NEXT_PUBLIC_JAW_API_KEY=

# Optional — get a project ID from https://cloud.reown.com to enable the
# WalletConnect (mobile QR) option. Leave blank to hide that row.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
